CREATE TABLE BANK_ACCOUNTS (
ACCOUNT NUMBER PRIMART KEY,
BANK_ACC_NO VARCHAR(30) UNIQUE,
RECON_TYPE VARCHAR(30),
CAT_CODE VARCHAR(20),
SUB_ACC_NO VARCHAR(30),
STATUS VARCHAR(10) DEFAULT 'ACTIVE'
);
